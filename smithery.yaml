runtime: typescript

startCommand:
  type: http
  configSchema:
    type: object
    properties:
      hyperFeedApiKey:
        type: string
        title: "HyperFeed API Key"
        description: "Your HyperFeed API key (optional)"